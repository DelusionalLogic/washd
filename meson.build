project(
	'washd', ['c'],
	version : '1.0.0',
	license : 'GPL',
	default_options : [
	]
)
inc = include_directories('include')

thread_dep = dependency('threads')

subdir('include')
subdir('washd')
subdir('washc')
subdir('test')
